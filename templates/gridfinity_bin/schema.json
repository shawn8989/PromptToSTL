{
  "template_id": "gridfinity_bin",
  "label": "Gridfinity Bin",
  "scad_file": "model.scad",
  "category": "Home & office",
  "icon": "🗃️",
  "description": "Gridfinity-compatible storage bin — pick grid units and height.",
  "params": {
    "grid_x": {
      "type": "integer",
      "default": 2,
      "min": 1,
      "max": 6,
      "label": "Grid units wide",
      "group": "Dimensions",
      "help": "Each unit is 42 mm — the Gridfinity standard."
    },
    "grid_y": {
      "type": "integer",
      "default": 1,
      "min": 1,
      "max": 6,
      "label": "Grid units deep",
      "group": "Dimensions",
      "help": "Each unit is 42 mm."
    },
    "units_h": {
      "type": "integer",
      "default": 3,
      "min": 1,
      "max": 12,
      "label": "Height units",
      "group": "Dimensions",
      "help": "Each unit is 7 mm tall, on top of the 4.75 mm base."
    },
    "wall": {
      "type": "number",
      "default": 1.2,
      "min": 0.8,
      "max": 3,
      "label": "Wall thickness",
      "unit": "mm",
      "group": "Dimensions",
      "help": "How thick the sides are. ~2.4 mm is watertight and strong."
    },
    "floor_th": {
      "type": "number",
      "default": 2.2,
      "min": 1.2,
      "max": 5,
      "label": "Floor thickness",
      "unit": "mm",
      "group": "Dimensions",
      "help": "How thick the bottom is."
    },
    "magnets": {
      "type": "integer",
      "default": 0,
      "min": 0,
      "max": 1,
      "label": "Add magnet pockets",
      "group": "Style",
      "help": "1 = add pockets underneath for magnets, 0 = none."
    }
  }
}
